# Crea un archivo README.md con la documentación completa
readme_content = """# SDAW_1722339 🌐

Una pequeña aplicación web desarrollada con **HTML**, **JavaScript** y **Node.js**.  
Este proyecto tiene como objetivo demostrar la estructura básica de una aplicación web moderna con un servidor simple, lógica del lado del cliente y empaquetado con **npm** y usando como ejemplo un boton que al pulsarlo nos salude con nuestro nombre.

---

## 📁 Estructura del proyecto

/SDAW_1722339
├── index.html # Página principal de la aplicación (interfaz de usuario)
├── script.js # Lógica del lado del cliente (interactividad)
├── server.js # Servidor Node.js (manejo de peticiones HTTP)
├── package.json # Dependencias, scripts y metadatos del proyecto
└── README.md # Documentación del proyecto

yaml
Copiar código

---

## 🚀 Descripción general

Esta aplicación web consiste en una página HTML servida desde un servidor **Node.js**.  
El servidor puede manejar solicitudes básicas (GET, POST, etc.) y servir contenido estático.

El archivo `script.js` se encarga de la lógica del cliente, como manejar eventos, procesar datos o comunicarse con el servidor mediante **fetch** o **AJAX**.

---

## 🧩 Tecnologías utilizadas

- **HTML5**
- **CSS3** (opcional)
- **JavaScript (ES6+)**
- **Node.js**
- **Express.js** (opcional)
- **npm**

---

## ⚙️ Instalación y ejecución

```bash
git clone https://github.com/usuario/SDAW_1722339.git
cd SDAW_1722339
npm install
node server.js
Luego abre http://localhost:3000 en tu navegador.

👨‍💻 Autor
Nombre: Diego
Correo: 1722339@alu.murciaeduca.es
GitHub: https://github.com/Patatuela1

💡 “Pequeños proyectos construyen grandes desarrolladores.”
"""
Comandos utilizados
Hemos utilizado estos comandos a lo largo de la practica
para montar el repositorio:
-> npm install express para instalar el servidor.
-> npm start para iniciar el servidor.
-> git init. para iniciar el repositorio.
-> git config user.name y git
       config user.email. para configurar el usuario local 
-> git config --list. para verificar la configuracion
-> (git status, git log, git diff y git restore, git revert). 
    para tener un seguimiento.
-> git checkout para crear dos ramas independientes de la original.
-> commit para publicar la rama 1 en el repositorio externo.
-> pull request para fusionar cada rama con la rama principal.
-> Git log para ver el historial del repositorio.
-> npm init -y. para crear el package.json


Mi conclusion final
Mi conclusion final es que esta practica esta bien para 
que se compruebe como se veria un caso real de hacer una pagina web usando
una IA para que nos genere el markdown (README) y lo necesario
para inicializar una pagina web, en resumen y lo que esta bien
es que la IA te ayuda a crear el markdown para no tener
que hacerlo manualmente tu, ahorrandote mucho tiempo 
aunque tambien tiene sus errores como cualquier cosa.
aunque tambien tiene sus errores como cualquier cosa.



## Información técnica añadida desde rama 1
En esta sección se describen los principales comandos de Git
utilizados durante la práctica:
- git init: (COMPLETAR)
- git add: (COMPLETAR)
- git commit: (COMPLETAR)
- git branch: (COMPLETAR)
- git merge: (COMPLETAR)
- git push: (COMPLETAR)

## Información técnica añadida desde rama 2
67498e2 (HEAD -> rama2_nombreApellido1Estudiante, main) Revert "Primer commit"
282f1c5 (origin/main) Primer commit.
